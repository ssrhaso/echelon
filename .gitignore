# --- Python ---
__pycache__
*.pyc
.ipynb_checkpoints
.cache

# --- Environments ---
venv/
.venv/

# --- Editors / tooling ---
.vscode
.idea
.DS_Store
.claude/
.agitrack/

# --- Training output (local only; never versioned) ---
# Checkpoints, replay buffers and raw stdout from training runs. The W&B run
# history is the source of truth for everything derived from them.
logs/
results/
callbacks/
artifacts/
checkpoints_local/
transfer_ckpt/
judgement/
isca_logs/
*.ckpt
*.log

# --- Analysis and figure pipeline (local only) ---
# This repository is the model codebase. The code that reads the W&B run
# history and the SLURM logs to rebuild the paper's tables, figures and
# checkpoint diagnostics is kept alongside the paper, not here.
scripts/
analysis_pta/
configs/diagnostics/

# --- Game-selection working data ---
# Regenerated by the selection screen; the published coordinates live in the
# game-properties table in the paper.
priori/

# --- Weights & Biases ---
wandb/
wandb_export/

# --- Audit / scratch output ---
runs_inventory.csv
scratchpad_*.txt
# Working notes: run audits, sprint plans, cluster references. Local by design.
notes/
# Run manifests are generated from the sweep definitions in experiments/.
experiments/isca_*manifest*.csv
experiments/isca_pilot_*.csv

# --- Cross-architecture IRIS port (local only) ---
# An exploratory port of the HRVQ tokenizer into IRIS. It is not part of the
# paper and is kept out of the companion codebase.
IRIS/

# --- Paper (local only) ---
# The repository is the companion codebase; the manuscript, its build products
# and internal reports/working notes stay local.
figures/
tex/
*.tex
*.sty
*.bib
*.pdf
*.aux
*.out
*.synctex.gz
*.bbl
*.blg
*.fls
*.fdb_latexmk
CLAIM_LEDGER.md
EXPERIMENT_GAP.md
PAPER_REPORT.md
REVISION_NOTES.md
style_lint_report.txt

# --- Internal planning / working notes (local only) ---
experiments/CODEBOOK_STITCHING_*.md
experiments/depth_ablation/

# --- Codebook-stitching sweep (local only) ---
# The stitching experiment did not survive verification and is not reported in
# the paper, so its sweep definition and launchers are kept out of the repo.
experiments/codebook_stitching.yaml
experiments/isca_stitch.slurm
experiments/setup_stitching.ps1

# --- Local-only smoke test runner under nnet/modules/twister/fvq/ ---
**/system_test.py
