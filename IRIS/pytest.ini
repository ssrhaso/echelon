[pytest]
# IRIS source modules use flat imports (e.g. `from dataset import Batch`,
# `from models.tokenizer import Tokenizer`), so src/ must be on sys.path.
# conftest.py inserts it; pythonpath here is a belt-and-braces fallback.
pythonpath = src
testpaths = tests
addopts = -q
