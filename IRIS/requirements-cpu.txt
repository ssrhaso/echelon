# CPU-only validation environment for the IRIS + 3-level HRVQ port.
# This is NOT the GPU/RL environment (see launch_package/ENV_BUILD.md for aarch64 GH200).
# These pins exist solely to run the CPU unit tests in this repo.
torch==2.11.0
torchvision==0.26.0
einops==0.8.1
hydra-core==1.3.2
omegaconf==2.3.0
numpy==2.2.6
pillow==11.3.0
pytest==8.4.1
# Import-time deps of the IRIS source modules we exercise on CPU
# (dataset->psutil, utils->cv2, tokenizer.lpips->requests/tqdm). LPIPS VGG
# weights are NOT downloaded in tests: tokenizer is built with_lpips=False
# and compute_loss tests stub the perceptual term.
psutil==5.9.8
opencv-python-headless==4.11.0.86
requests==2.32.3
tqdm==4.66.4
